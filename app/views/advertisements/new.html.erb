<h1>New Ad</h1>

<div class="row">
  <div>
    <p>Make sure that your ad targets our market.</p>
    <ul>
      <li>Only certified brands can post ads.</li>
      <li>Make sure to upload the right ad.</li>
    </ul>
  </div>
</div>

<div class="col-md-8">
  <%= form_for @advertisement do |f| %>
  <div class="form-group">
    <%= f.label :title %>
    <%= f.text_field :title, class: 'form-control', placeholder: "Enter your Ad title" %>
  </div>

  <div class="form-group">
    <%= f.label :text %>
    <%= f.text_field :text, class: 'form-control', placeholder: "Enter Ad text" %>
  </div>

  <div class="form-group">
    <%= f.label :price %>
    <%= f.text_field :price, class: 'form-control', placeholder: "Enter the amount that you want to spend" %>
  </div>

  <div class="form-group">
    <%= f.submit "Save", class: 'btn btn-success' %>
  </div>
  <% end %>
</div>
